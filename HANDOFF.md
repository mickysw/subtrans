# 인수인계 — 다음 사람(또는 다음 AI)이 먼저 읽을 것

이 문서는 **대화 기록 없이 이 저장소만 열었을 때** 바로 이어서 작업할 수 있게 쓴 것이다.
설계 근거는 커밋 메시지와 코드 주석에 남겨두었으니, 막히면 `git log`를 읽는 것이 가장 빠르다.

---

## 0. 실행 방법 — 이것부터

**`.venv\Scripts\python.exe` 를 직접 부르지 마라.** Windows Smart App Control 이
uv 가 만든 서명 없는 실행기를 차단한다(세션 도중에 갑자기 막혔다).

```bash
./py.cmd shorts/candidates.py J7AjoLyoABw     # 이렇게 쓴다
```

`py.cmd` 는 서명된 CPython 을 쓰되 venv 의 패키지 경로를 물린다. 다른 PC라면
`py.cmd` 안의 `PYBASE` 경로만 고치면 된다(`uv python find 3.12` 로 확인).

⚠️ **`py.cmd` 에 여러 줄짜리 `-c` 코드를 넘기면 cmd.exe 가 삼킨다.** 임시 스크립트
파일을 만들어 실행할 것.

---

## 1. 이 프로젝트가 하는 일

영어 인터뷰 영상 → ① 한국어 자막을 구운 롱폼 mp4 → ② 그 롱폼에서 하이라이트를 뽑은 쇼츠.

- **롱폼 파이프라인은 완성**돼 있고 26분 영상 한 편을 끝까지 검증했다. `README.md` 참고.
- **쇼츠 파이프라인 ①~④ 완료.** 손흥민 인터뷰 `J7AjoLyoABw`에서 6편을 렌더했다.
  설계는 `docs/superpowers/specs/2026-09-08-shorts-design.md`.

---

## 2. 지금 어디까지 됐나

### 롱폼 (완료)
`run.py <유튜브주소>` → 받기·받아쓰기·조각내기·번역·검사·검수화면 → `--burn` 으로 굽기.

### 쇼츠 (6편 렌더 완료, 업로드 전 검수 남음)

| | 단계 | 파일 | 상태 |
|---|---|---|---|
| ① | 후보 선정 | `shorts/candidates.py` | 완료 |
| ② | 편집 결정(EDL) | `shorts/edl.py` | 완료 |
| ③ | 검수 화면 | `shorts/review_page.py` | 완료 |
| ④ | **렌더** | `shorts/render.py` | 완료 |
| 2차 | 얼굴 추적 | `shorts/framing.py` | 없음 (선택 과제) |

`work/J7AjoLyoABw/shorts/` 에 후보·EDL·완성 mp4가 6개씩 있다.
2026-09-16에 제목 88px, 노란 핵심어 강조, 하단 자막 32px 올리기를 반영했다.
새 후보는 기존 후보 선정 AI 호출에서 강조어를 함께 받아 추가 호출이 없다.
이 6편의 강조어는 `candidates.json`에 저장해 렌더 시 AI를 다시 호출하지 않는다.

---

## 3. 렌더 구조와 다음 검수

### 입력
- `work/<id>/shorts/edl_<n>.json` — 어느 조각을 어떤 순서로 이어 붙일지
- `work/<id>/video.mkv` — 원본 (1920x1080 VP9)
- `work/<id>/ko.srt` (또는 `ko.edited.srt`) — 자막
- `work/<id>/shorts/selected.json` — 사람이 고른 번호 (없으면 전부)

### 배치 — 1080 x 1920

```
┌────────────────────┐  y=0
│   제목 (최대 2줄)    │   430px
├────────────────────┤  y=430
│      영 상          │  1080px   ← 원본에서 1080x1080 잘라냄
│   1080 x 1080      │              확대·축소 없음
├────────────────────┤  y=1510
│   자막 (최대 2줄)    │   410px
└────────────────────┘  y=1920
```

**왜 1080x1080인가**: 원본이 1920x1080이고 쇼츠 폭이 1080이다. 세로를 통째로 쓰고
가로만 잘라내면 **리샘플링이 0**이다. 화질이 공짜로 유지된다. 이 숫자를 바꾸면 그 이점이 사라진다.

현재 가로 크롭은 가운데로 고정한다. 얼굴 추적은 아직 구현하지 않았다.

### 두 단계로 나눠 굽는다 — 한 번에 하지 말 것

한 번에 하면 재배치·크롭·자막 시각이 얽혀 원인을 못 찾는다.

**4-a** `edl_<n>.json` 의 `spans` 를 `trim`+`concat` → `clip_<n>_raw.mp4` (crf 10, 거의 무손실).
이 파일의 타임라인이 최종 타임라인이라 이후 계산이 단순해진다.

**4-b** `crop` → `pad` → `ass` → h264 crf 18, 오디오 AAC 복사 → `<n>_<제목>.mp4`

### 자막 시각
`edl_<n>.json` 의 `cue_spans`(출력 타임라인 기준)를 그대로 쓴다. **재번역하지 않는다** —
`ko.srt` 에서 cue 번호로 가져오면 된다.

### 재사용할 것
- `steps/burn.py` 의 `build_ass()`, `safe_filename()`, 인코딩 설정
- `steps/srtlib.py` 의 `read_srt()`
- `steps/translate.py` 의 `Translator` (LLM 호출·JSON 파싱·재시도)

### 결과와 남은 확인
- `<n>_<제목>.txt` 설명란 초안도 각 영상과 함께 생성된다.
- 6편 모두 1080x1920이며 39~59초. 1번과 6번은 프레임을 눈으로 확인했다.
- 2번은 EDL 예상 길이보다 실제 이어 붙인 영상이 0.26초 길다는 경고가 있다.
  업로드 전 그 영상의 끝부분 음성·자막 싱크를 재생해서 확인할 것.
- 6편의 이음새와 자막 가독성을 휴대전화에서도 확인할 것. 아직 실제 폰 검수는 안 했다.
- `python -B shorts/render.py J7AjoLyoABw 6 --force --reuse-raw`로 기존 중간
  파일을 재사용해 6번의 제목·자막만 다시 구울 수 있다.

---

## 4. 반드시 알아야 할 함정

### 코드

1. **cue 번호는 1부터 센다** (`ko.srt` 와 맞추려고). 배열 색인은 `-1`.
   섞이면 자막이 한 칸씩 밀린다. 경계에서만 변환할 것.

2. **후킹 선정은 한국어 자막으로 판단해야 한다.** 영어 원문으로 시켰더니
   4개 중 4개가 거절됐다 — 대화체 영어는 대부분 `and/but/so` 로 시작해
   "자립적이지 않다"는 규칙에 전부 걸린다. 한국어로 바꾸니 4/4 통과.
   기준도 "자립성"이 아니라 **"궁금증을 일으키는가"** 다.

3. **단어 안쪽은 절대 자르지 않는다.** 무음을 덜어낼 때 남기는 숨은
   **그 자리에 실제로 있는 무음의 절반을 넘지 않아야** 한다 — 넘으면 옆 단어를 삼킨다.
   (`shorts/edl.py` 의 `gaps_around()`)

4. **파이썬 패치 스크립트를 heredoc 으로 넘기지 마라.** 치환 문자열 안의 `\n` 이
   실제 줄바꿈으로 변해 f-string 이 깨진다. 이 세션에서 세 번 당했다.
   파일을 직접 쓰는 편이 빠르다.

5. **yt-dlp 출력은 UTF-8 로 강제해야 한다.** 안 그러면 제목의 특수문자가 깨져
   파일 이름에 `U+FFFD` 가 박힌다. `steps/fetch.py` 의 `run()` 참고.

### 화질 판단

6. **VMAF 게이트는 "화질이 좋다"의 기준이 아니라 "설정이 망가졌다"의 기준이다.**
   임계값 80. 점수가 낮다고 CRF 를 쫓지 마라 — 17→11 로 내려도 0.37점만 오른다(두 번 측정함).
   이미 압축된 영상을 재인코딩하면 인코더가 원본의 압축 잡음을 다듬는데,
   눈에는 안 보여도 VMAF 는 손상으로 센다. 낮으면 **해상도가 줄었는지, 프레임레이트가
   같은지**를 확인하라.

7. **측정할 때 `-ss` 로 탐색하지 마라.** 두 파일의 키프레임 위치가 달라 1프레임씩 어긋나고,
   같은 구간이 85.1 vs 97.4 로 갈린다. 처음부터 디코딩하고 `select` 필터로 구간을 고를 것.

---

## 5. 이미 정해진 것 — 다시 논의하지 말 것

- **Content ID 회피는 만들지 않는다.** 액자를 둘러도 막을 수 없고(크기 변경·크롭에
  견디도록 설계됨, 오디오 지문은 더 강함), 탐지 회피가 목적인 작업이라서다.
  액자는 브랜딩과 시청 경험을 위한 것이다.
- **공개 웹 서비스로 배포하지 않는다.** 유튜브가 클라우드 IP 에서의 다운로드를 막고,
  서버비가 들고, 법적 책임이 운영자에게 온다. 로컬 도구로 둔다.
- **자동 업로드를 만들지 않는다.** 검수 후 사람이 올린다.
- **영·한 자막 병기는 지금 하지 않는다.** 재료(`en.srt`)는 같은 타이밍으로 있으니
  나중에 켜기 쉽다.
- **통통 튀는 자막 애니메이션을 넣지 않는다.**
- **얼굴 추적은 정확도 게이트를 통과할 때만 켠다.** 틀리면 안 하느니만 못하다는 판단.
  기본은 정적 크롭.

## 6. 사용자가 정한 방향

- 채널 성격: **"정상에 오른 사람들의 이야기"** — 인물이 매번 바뀌므로
  액자는 인물과 무관한 중립 디자인이어야 한다(로고 + 출처).
- 목표는 광고 수익. 그런데 원본 채널의 허락 없이는 클레임/신고 위험이 남는다.
  **허락받은 영상, 크리에이티브 커먼즈, 공공기관 자료 위주로 쓰는 방향**으로 정리 중.
- 자막 말투: 한국어 구어체. 문어체 금지.

---

## 7. 검증 방법

```bash
./py.cmd shorts/render.py J7AjoLyoABw          # 이미 있는 결과는 재사용
./py.cmd shorts/render.py J7AjoLyoABw 6 --force --reuse-raw  # 6번 스타일만 다시 굽기
```

- `ffprobe` 로 출력이 **정확히 1080x1920** 인지 확인. 이어 붙이는 과정의 프레임
  반올림으로 EDL 예상 길이와 실제 길이가 조금 다를 수 있다(2번은 0.26초).
- **영상 영역이 원본과 픽셀 단위로 같은지** — 1080x1080 크롭이 확대·축소 없이 들어갔다면
  거의 일치해야 한다. 다르면 어딘가에서 스케일이 끼어든 것이다
- **이음새를 귀로 듣는다** — 후킹 문장 뒤에 원래 이야기가 붙는 자리. 이게 이 기능의
  핵심이고 글로는 판단이 안 된다
- 자막이 아래 띠 안에 들어오는지, 제목이 잘리지 않는지
- **폰에서 재생해 글씨가 읽히는지** — 데스크톱에서 크게 보이는 것과 다르다

---

## 8. 더 읽을 것

| 무엇 | 어디 |
|---|---|
| 롱폼 설계와 실측 기록 | `README.md` (한국어), `README.en.md` (영어) |
| 쇼츠 전체 설계 | `docs/superpowers/specs/2026-09-08-shorts-design.md` |
| 왜 그렇게 고쳤는지 | `git log` — 커밋 메시지에 근거를 적어두었다 |
| 편집 규칙의 이유 | `shorts/edl.py` 상단 주석 |
